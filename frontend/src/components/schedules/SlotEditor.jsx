// SlotEditor is no longer needed - schedules are now flat records.
// This component is kept as a pass-through to ScheduleForm for backward compatibility.
// All slot editing is handled directly in ScheduleForm.

export default function SlotEditor() {
  return null
}
